# frozen_string_literal: true

RSpec.describe Frame9::Jekyll::Theme do
  it "has a version number" do
    expect(Frame9::Jekyll::Theme::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
